# Story 1.6: Metrics Sync Routine

## Status

Ready
**Complexity:** M (Medium)

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["typescript", "vitest"]
```

**Epic:** EPIC-001 Platform Foundation
**Position:** 6ª story do EPIC-001
**Bloco coberto:** Bloco 4A (sync de métricas via Routine)
**Dependência:** Stories 1.4 + 1.5 (contas conectadas com tokens válidos), `routines` (existente)

## Story

**As a** empresa do Paperclip,
**I want** que as métricas das minhas contas sociais (follower_count, avg_engagement_rate) sejam sincronizadas automaticamente via cron,
**so that** o dashboard mostre dados atualizados sem que eu precise clicar em "atualizar" manualmente.

## Acceptance Criteria

1. Existe Routine cadastrada (via `routines` table existente) para sync de métricas de contas sociais.
2. Routine tem `routine_triggers` com cron expression (padrão sugerido: `0 */6 * * *` — a cada 6 horas).
3. Routine é por empresa (`company_id`) — cada empresa tem sua própria Routine independente.
4. Execução da Routine itera por `company_social_accounts` `WHERE company_id = X AND is_active = true`.
5. Para cada conta, executa fetch na API da plataforma (Instagram Graph API nesta story) usando o token do `company_secrets` referenciado por `secret_id`.
6. Atualiza campos `follower_count`, `avg_engagement_rate`, `last_synced_at` na tabela `company_social_accounts`.
7. Erros são logados em `heartbeat_run_events` (não falham a Routine inteira — uma conta com problema não impede sync das outras).
8. Token expirado dispara tentativa de refresh (se `refresh_token` disponível no secret). Falha de refresh marca conta como `needs_reauth` (campo novo opcional ou via `metadata` JSONB).
9. Rate limit da plataforma respeitado — implementar backoff exponencial em caso de 429.
10. Endpoint manual `POST /api/company/social-accounts/:id/sync` permite trigger manual da sync para uma conta específica (útil para debug e UX).
11. `pnpm run typecheck`, `pnpm run build`, `pnpm run test` passam.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled — manual review.

## Tasks / Subtasks

- [ ] **Task 1: Agente de sync** (AC: 4, 5, 6)
  - [ ] Criar arquivo/módulo para o agente de sync (verificar padrão de agentes em `server/src/`)
  - [ ] Implementar função `syncMetricsForCompany(companyId)`:
    - Query contas ativas
    - Para cada conta: resolve secret → fetch Instagram Graph `/me?fields=followers_count,...`
    - Calcula `avg_engagement_rate` baseado em últimas N posts (lib de cálculo simples)
    - Update tabela com novos valores + `last_synced_at = now()`

- [ ] **Task 2: Criar Routine + trigger** (AC: 1, 2, 3)
  - [ ] Criar Routine via API existente (`server/src/routes/routines.ts`) ou seed
  - [ ] `routine_trigger` com `kind: 'cron'`, `cron_expression: '0 */6 * * *'`
  - [ ] Documentar como Super Admin/empresa pode customizar o cron via UI futura

- [ ] **Task 3: Tratamento de erros + token refresh** (AC: 7, 8, 9)
  - [ ] Wrap fetch em try/catch — log em `heartbeat_run_events` (verificar API existente)
  - [ ] Detectar 401/403 → tentar refresh do token via Instagram OAuth refresh endpoint
  - [ ] Detectar 429 → backoff exponencial (1s, 2s, 4s, 8s, max 60s)
  - [ ] Marcar conta como `needs_reauth = true` (em metadata JSONB ou campo dedicado) se refresh falhar

- [ ] **Task 4: Endpoint manual de sync** (AC: 10)
  - [ ] `POST /api/company/social-accounts/:id/sync`
  - [ ] Auth: membro da empresa
  - [ ] Dispara `syncMetricsForCompany` apenas para a conta específica
  - [ ] Retorna métricas atualizadas

- [ ] **Task 5: Testes**
  - [ ] Mock Instagram API
  - [ ] Test: sync atualiza `last_synced_at` e métricas
  - [ ] Test: erro em 1 conta não impede sync das outras
  - [ ] Test: 401 dispara refresh; falha de refresh marca `needs_reauth`

- [ ] **Task 6: Validação final**
  - [ ] `pnpm run typecheck`, `pnpm run build`, `pnpm run test`

## Dev Notes

### Decisão registrada
`docs/plans/content-brand-module-brainstorm.md` Bloco 4A (2026-05-30): "Sync de métricas é uma **Routine** com cron trigger. Infraestrutura já existe e é madura."

### Infra existente a reusar
- `routines` + `routine_triggers` + `routine_runs` (schema completo em `packages/db/src/schema/routines.ts`)
- `heartbeat_runs` + `heartbeat_run_events` para logging
- `company_secrets` para resolver tokens

### NÃO criar
- ❌ Novo sistema de jobs/cron — usar `routines` existente
- ❌ Novo storage de tokens — sempre via `company_secrets`

### Plataforma única nesta story
Apenas Instagram (consistência com Story 1.5). YouTube, LinkedIn etc. seguem padrão idêntico em stories futuras.

### Out of scope
- ❌ Métricas avançadas (impressions, reach, demographics) → fase futura
- ❌ Dashboard de métricas → Epic 3 ou separado
- ❌ Notificação ao usuário quando `needs_reauth` → fase futura

### Testing
- Vitest para lógica
- Mock fetch para API Instagram
- Test isolamento por `company_id`

## Change Log

| Date       | Version | Description    | Author          |
|------------|---------|----------------|-----------------|
| 2026-05-30 | 1.0     | Story drafted from EPIC-001 (batch mode) | @sm (River) |
| 2026-05-30 | 1.1     | PO validation passed (8/10), status Approved→Ready, complexity M | @po (Pax) |

## Dev Agent Record
_To be populated by @dev._

## QA Results
_To be populated by @qa._
