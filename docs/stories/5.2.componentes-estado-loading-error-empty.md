# Story 5.2: Componentes de Estado — Loading / Error / Empty

## Status

Ready
**Complexity:** M (Medium)

## Executor Assignment

```yaml
executor: "@ux-design-expert"
quality_gate: "@dev"
quality_gate_tools: ["visual_regression", "a11y_audit", "manual_review"]
```

**Epic:** EPIC-005 Tech Debt — Frontend Performance & UX Debt  
**Position:** 2ª story do EPIC-005  
**Bloco coberto:** Bloco B — Estados de Loading/Error/Empty (F03–F08)  
**Dependência:** Story 5.1 (pode ser paralela — não há dependência técnica direta)

## Story

**As a** usuário do Paperclip,  
**I want** que todas as páginas mostrem estados de carregamento, erro e vazio padronizados,  
**so that** eu nunca veja uma tela em branco ou silêncio sem feedback — sempre saberei o que está acontecendo com o sistema.

## Acceptance Criteria

1. Componente `<ErrorState>` é criado em `ui/src/components/ErrorState.tsx` com props: `message: string`, `retry?: () => void`, `details?: string` — renderiza mensagem de erro, detalhe opcional e botão de retry
2. Componente `<EmptyState>` existente em `ui/src/components/EmptyState.tsx` é ampliado: props `action` e `onAction` passam a aceitar `ReactNode` além de `string`, mantendo backward compatibility com callers existentes
3. O ícone `LucideIcon` em `EmptyState` recebe `aria-hidden={true}` (corrige problema de acessibilidade F13 parcial)
4. `<ErrorState>` é aplicado nas seguintes páginas/componentes, **substituindo** os padrões inline de `text-destructive`:
   - `ui/src/pages/Agents.tsx` (AgentList — estado de erro de listagem)
   - `ui/src/components/IssuesList.tsx` (IssueList — estado de erro de listagem)
   - `ui/src/pages/Costs.tsx` (CostDashboard — erro de fetch de custos)
   - `ui/src/pages/Approvals.tsx` (ApprovalsQueue — erro de fetch de aprovações)
   - `ui/src/pages/CompanySettings.tsx` (erro de fetch/update de settings)
5. `<EmptyState>` já aplicado ou sem estado vazio é verificado nos 6 alvos — onde ausente, o componente existente é aplicado com ícone, mensagem e CTA adequados
6. Toast notifications em `ui/src/components/ToastViewport.tsx` recebem `role="status"` (corrige F15 — acessibilidade de toasts)
7. `pnpm test:run` passa sem regressões — testes existentes de `Agents`, `IssuesList`, `CompanySettings` devem continuar passando
8. `pnpm build` completa sem warnings
9. Nenhuma prop existente de `EmptyState` é quebrada — todos os callers atuais continuam funcionando sem modificação

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Pre-Commit + Pre-PR
>
> - Pre-Commit: Componentes renderizam em todos os estados (loading/error/empty)
> - Pre-PR: Review visual do @ux-design-expert nos 6 alvos

## Tasks / Subtasks

- [ ] **Task 1: Mapear estado atual dos 6 alvos** (AC: 4, 5)
  - [ ] Para cada alvo, identificar: (a) se tem loading state, (b) se tem error state, (c) se tem empty state
  - [ ] Registrar achados no Change Log no formato:
    ```
    Agents.tsx: loading=PageSkeleton✓, error=inline text-destructive✗, empty=EmptyState✓
    IssuesList.tsx: loading=..., error=..., empty=...
    Costs.tsx: loading=..., error=..., empty=...
    Approvals.tsx: loading=..., error=..., empty=...
    CompanySettings.tsx: loading=..., error=..., empty=...
    ```
  - [ ] Verificar arquivos de test existentes que mockam esses estados

- [ ] **Task 2: Criar `<ErrorState>` em `ui/src/components/ErrorState.tsx`** (AC: 1)
  - [ ] Props interface:
    ```tsx
    interface ErrorStateProps {
      message: string;
      details?: string;
      retry?: () => void;
    }
    ```
  - [ ] Estrutura visual:
    - Ícone `AlertCircle` de lucide-react com `aria-hidden={true}`
    - `<p>` com `message` em `text-sm text-destructive`
    - `<p>` condicional com `details` em `text-xs text-muted-foreground`
    - `<Button variant="outline" size="sm">` com "Tentar novamente" se `retry` fornecido
  - [ ] Usar apenas tokens CSS existentes de `ui/src/index.css` — sem cores hardcoded
  - [ ] Garantir que o componente renderiza corretamente sem props opcionais

- [ ] **Task 3: Atualizar `<EmptyState>` em `ui/src/components/EmptyState.tsx`** (AC: 2, 3, 9)
  - [ ] Ampliar tipos de `action` e `onAction`:
    ```tsx
    // ANTES:
    action?: string;
    onAction?: () => void;
    // DEPOIS (backward compatible):
    action?: string | ReactNode;
    onAction?: () => void;
    ```
  - [ ] Adicionar `aria-hidden={true}` no `<Icon>`:
    ```tsx
    <Icon className="h-10 w-10 text-muted-foreground/50" aria-hidden={true} />
    ```
  - [ ] Verificar que todos os callers existentes de `EmptyState` ainda compilam sem erros (`pnpm -r typecheck`)

- [ ] **Task 4: Aplicar `<ErrorState>` nos 6 alvos** (AC: 4)
  - [ ] **`ui/src/pages/Agents.tsx`**: Localizar `{error && ...}` inline → substituir por `<ErrorState message="..." retry={refetch} />`
  - [ ] **`ui/src/components/IssuesList.tsx`**: Localizar error handling inline → substituir por `<ErrorState>`
  - [ ] **`ui/src/pages/Costs.tsx`**: Localizar error state (possivelmente ausente) → adicionar `<ErrorState>` no branch de error do TanStack Query
  - [ ] **`ui/src/pages/Approvals.tsx`**: Idem
  - [ ] **`ui/src/pages/CompanySettings.tsx`**: Idem — atenção para múltiplos `useQuery` no arquivo
  - [ ] Padrão TanStack Query a seguir:
    ```tsx
    const { data, isLoading, isError, error, refetch } = useQuery(...)
    if (isLoading) return <PageSkeleton variant="..." />
    if (isError) return <ErrorState message={error.message} retry={refetch} />
    ```

- [ ] **Task 5: Aplicar `<EmptyState>` nos alvos que faltam** (AC: 5)
  - [ ] Para cada alvo sem empty state, adicionar `<EmptyState>` quando `data?.length === 0` (ou equivalente)
  - [ ] Usar ícones Lucide semanticamente relevantes por contexto (ex: `AgentList` → `Bot`, `ApprovalQueue` → `CheckSquare`)

- [ ] **Task 6: Adicionar `role="status"` em ToastViewport** (AC: 6)
  - [ ] Abrir `ui/src/components/ToastViewport.tsx`
  - [ ] Verificar se já existe `role` ou `aria-live` — se não:
    - Adicionar `role="status"` e `aria-live="polite"` ao elemento container dos toasts
  - [ ] Não alterar o comportamento visual

- [ ] **Task 7: Validar** (AC: 7, 8)
  - [ ] `pnpm test:run` — foco em testes de `Agents`, `IssuesList`, `CompanySettings` e qualquer teste que renderize `EmptyState`
  - [ ] `pnpm build` — sem warnings
  - [ ] `pnpm -r typecheck` — zero erros
  - [ ] Verificação visual: navegar para cada página modificada e validar que loading/error/empty aparecem corretamente

## Dev Notes

### Contexto da Story

Esta story cria e padroniza os estados fundamentais de UI que estão ausentes ou inconsistentes em 6 páginas críticas. O padrão atual usa inline `text-destructive` sem retry, sem estrutura consistente. [Source: docs/frontend/frontend-spec.md#7.2]

### Componentes existentes relevantes

**`EmptyState.tsx`** já existe em `ui/src/components/`:
```tsx
// Props atuais:
interface EmptyStateProps {
  icon: LucideIcon;
  message: string;
  action?: string;
  onAction?: () => void;
}
// Problema: action é só string, ícone sem aria-hidden
```
[Source: ui/src/components/EmptyState.tsx verificado]

**`PageSkeleton.tsx`** já existe com 8 variantes (list, issues-list, detail, dashboard, approvals, costs, inbox, org-chart). Esta story NÃO adiciona variantes — usa as existentes.
[Source: ui/src/components/PageSkeleton.tsx verificado]

**`ErrorState`** NÃO existe — deve ser criado do zero.
[Source: frontend-spec.md#7.2 — "sem ErrorState ou ErrorBoundary em lugar algum"]

### Design System — tokens disponíveis

```css
/* Para ErrorState usar: */
--destructive       /* cor de erro */
--muted-foreground  /* texto secundário */
--muted             /* background sutil */
--border            /* bordas */
```
[Source: docs/frontend/frontend-spec.md#2.2]

Design majoritariamente flat — `--radius: 0`. Não adicionar border-radius nos novos componentes a não ser que use `--radius-sm`.

### Stack React — padrões existentes

- **TanStack Query:** `useQuery` retorna `{ data, isLoading, isError, error, refetch }` — usar esse padrão nos 6 alvos
- **Tailwind v4:** `@theme inline` + variáveis CSS — usar tokens via `bg-muted`, `text-destructive`, etc.
- **shadcn/ui Button:** `<Button variant="outline" size="sm">` para retry action
- **Lucide icons:** importar individualmente (`import { AlertCircle } from "lucide-react"`)

### Callers existentes de EmptyState (não quebrar)

Verificar via grep antes de alterar props:
```bash
grep -r "EmptyState" ui/src --include="*.tsx" -l
```

### Decisões fixas (não eliciar)

- **Sem ErrorBoundary:** Esta story não implementa Error Boundary (React class component) — apenas o componente `<ErrorState>` funcional para estados de fetch error
- **Sem design system storybook:** Projeto não tem Storybook — validação é visual no app rodando
- **Backward compatible:** `EmptyState` não pode quebrar nenhum caller existente

### Out of scope

- ❌ Loading state dos 6 alvos (PageSkeleton já existe e está aplicado — verificar na Task 1, mas não é foco desta story)
- ❌ ErrorBoundary global → backlog técnico separado
- ❌ Empty state de `RunHistory` — componente não localizado no mapeamento inicial; verificar na Task 1

## Change Log

| Date       | Version | Description    | Author          |
|------------|---------|----------------|-----------------|
| 2026-06-17 | 1.0     | Story drafted from EPIC-005 | @sm (River) |
| 2026-06-17 | 1.1     | Fix: removido storybook_review (projeto sem Storybook) | @po (Pax) |
| 2026-06-17 | 1.2     | Validated GO (10/10) — Status: Draft → Ready | @po (Pax) |

## Dev Agent Record

### Agent Model Used

_A preencher pelo @ux-design-expert ao iniciar implementação_

### Debug Log References

_A preencher durante implementação_

### Completion Notes List

_Registrar mapeamento de Task 1 aqui antes de prosseguir para Task 2+_

### File List

| File | Action | Description |
|------|--------|-------------|
| `ui/src/components/ErrorState.tsx` | Created | Novo componente de estado de erro |
| `ui/src/components/EmptyState.tsx` | Modified | action aceita ReactNode; aria-hidden no ícone |
| `ui/src/pages/Agents.tsx` | Modified | ErrorState aplicado |
| `ui/src/components/IssuesList.tsx` | Modified | ErrorState aplicado |
| `ui/src/pages/Costs.tsx` | Modified | ErrorState aplicado |
| `ui/src/pages/Approvals.tsx` | Modified | ErrorState aplicado |
| `ui/src/pages/CompanySettings.tsx` | Modified | ErrorState aplicado |
| `ui/src/components/ToastViewport.tsx` | Modified | role="status" adicionado |

## QA Results

**Veredicto:** _Pendente_  
**QA Agent:** @dev | _Data a preencher_

| Check | Resultado | Notas |
|-------|-----------|-------|
| `<ErrorState>` renderiza em todos os estados | ⏳ Pendente | message, com/sem retry, com/sem details |
| `<EmptyState>` backward compatible | ⏳ Pendente | Callers existentes sem modificação |
| `aria-hidden` no ícone do EmptyState | ⏳ Pendente | |
| `role="status"` no ToastViewport | ⏳ Pendente | |
| ErrorState aplicado nos 5 alvos | ⏳ Pendente | Agents, Issues, Costs, Approvals, CompanySettings |
| `pnpm test:run` | ⏳ Pendente | |
| `pnpm build` sem warnings | ⏳ Pendente | |
| Visual review em cada página | ⏳ Pendente | Todos os 3 estados por página |
