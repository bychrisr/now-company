# Story 5.1: Code Splitting & Lazy Loading (AgentDetail + IssueChatThread)

## Status

Draft
**Complexity:** M (Medium)

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@ux-design-expert"
quality_gate_tools: ["bundle_analysis", "lighthouse_audit", "visual_regression"]
```

**Epic:** EPIC-005 Tech Debt — Frontend Performance & UX Debt
**Position:** 1ª story do EPIC-005
**Bloco coberto:** Bloco A — Bundle Size Critical (F01, F02)
**Dependência:** Nenhuma (pode iniciar imediatamente)

## Story

**As a** usuário do Paperclip,
**I want** que as páginas carreguem mais rapidamente,
**so that** eu tenha uma experiência fluída mesmo quando navego para seções complexas como Detalhes do Agente e Chat de Issues.

## Acceptance Criteria

1. `AgentDetail.tsx` é convertido para carregamento via `React.lazy()` + `Suspense` nas suas 3 rotas em `App.tsx` (linhas 98–100: `agents/:agentId`, `agents/:agentId/:tab`, `agents/:agentId/runs/:runId`)
2. `IssueChatThread.tsx` (componente em `ui/src/components/`) é convertido para `React.lazy()` + `Suspense` no ponto de consumo em `IssueDetail.tsx`
3. O Suspense boundary de `AgentDetail` usa `<PageSkeleton variant="agent-detail" />` — nova variante adicionada ao componente existente `ui/src/components/PageSkeleton.tsx`
4. O Suspense boundary de `IssueChatThread` usa `<PageSkeleton variant="issue-chat" />` — nova variante adicionada ao componente existente `ui/src/components/PageSkeleton.tsx`
5. Os skeletons das duas variantes novas mantêm o layout da página (sem flash branco/vazio) — estrutura visual correspondente ao layout real do componente
6. `pnpm build` produz análise de bundle via `rollup-plugin-visualizer` (ou equivalente já existente) que demonstra redução de ≥ 40% no chunk principal em relação ao baseline pré-task
7. `pnpm build` completa sem warnings de bundle size
8. `pnpm test:run` passa sem regressões
9. Navegação para as páginas refatoradas (`/agents/:id`, `/agents/:id/:tab`, `/agents/:id/runs/:runId`, `IssueDetail` com thread) funciona identicamente ao comportamento anterior
10. LCP (Largest Contentful Paint) não aumenta mais que 200ms medido via Lighthouse em relação ao baseline documentado antes desta task

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Pre-Commit + Pre-PR
>
> - Pre-Commit: Bundle size antes/depois (`vite build --mode production`)
> - Pre-PR: LCP e FID no Lighthouse não devem regredir

## Tasks / Subtasks

- [ ] **Task 1: Identificar e mapear imports e rotas atuais** (AC: 1, 2)
  - [ ] Localizar import de `AgentDetail` em `ui/src/App.tsx` (linha 11) — confirmar as 3 rotas afetadas (linhas 98–100)
  - [ ] Localizar import de `IssueChatThread` em `ui/src/pages/IssueDetail.tsx` — confirmar ponto exato de consumo
  - [ ] Verificar se algum outro arquivo além de `IssueDetail.tsx` importa diretamente `IssueChatThread` (seria breaking change) via `grep -r "IssueChatThread" ui/src --include="*.tsx" --include="*.ts" -l`
  - [ ] Verificar se algum outro arquivo além de `App.tsx` importa diretamente `AgentDetail` via `grep -r "AgentDetail" ui/src --include="*.tsx" --include="*.ts" -l`
  - [ ] Documentar baseline de bundle size: executar `pnpm build` e registrar tamanho do chunk principal no Change Log desta story

- [ ] **Task 2: Adicionar variantes de skeleton ao `PageSkeleton.tsx` existente** (AC: 3, 4, 5)
  - [ ] Abrir `ui/src/components/PageSkeleton.tsx` — entender variantes existentes como referência de padrão visual
  - [ ] Adicionar variante `"agent-detail"` ao tipo `PageSkeletonProps` e à lógica de renderização — estrutura deve espelhar o layout de `AgentDetail.tsx` (header com nome do agente + tabs + painel de runs)
  - [ ] Adicionar variante `"issue-chat"` ao tipo `PageSkeletonProps` e à lógica de renderização — estrutura deve espelhar o layout do thread de chat (área de mensagens + input area)
  - [ ] Usar apenas `<Skeleton>` de `@/components/ui/skeleton` com classes Tailwind — seguir padrão idêntico ao existente

- [ ] **Task 3: Converter `AgentDetail` para lazy loading** (AC: 1, 7, 9)
  - [ ] Em `ui/src/App.tsx`, substituir:
    ```tsx
    // ANTES (linha 11):
    import { AgentDetail } from "./pages/AgentDetail";
    // DEPOIS (topo do arquivo, após imports React):
    const AgentDetail = React.lazy(() =>
      import("./pages/AgentDetail").then((m) => ({ default: m.AgentDetail }))
    );
    ```
  - [ ] Envolver as 3 rotas de `AgentDetail` em um único `<Suspense fallback={<PageSkeleton variant="agent-detail" />}>` no `App.tsx`
  - [ ] Confirmar que `React` está importado no `App.tsx` (necessário para `React.lazy`)

- [ ] **Task 4: Converter `IssueChatThread` para lazy loading** (AC: 2, 7, 9)
  - [ ] Em `ui/src/pages/IssueDetail.tsx`, substituir o import direto de `IssueChatThread`:
    ```tsx
    // ANTES:
    import { IssueChatThread } from "@/components/IssueChatThread";
    // DEPOIS:
    const IssueChatThread = React.lazy(() =>
      import("@/components/IssueChatThread").then((m) => ({ default: m.IssueChatThread }))
    );
    ```
  - [ ] Envolver o uso de `<IssueChatThread>` em `IssueDetail.tsx` com `<Suspense fallback={<PageSkeleton variant="issue-chat" />}>`
  - [ ] Confirmar que `React` está importado em `IssueDetail.tsx`

- [ ] **Task 5: Medir e documentar impacto de bundle** (AC: 6, 7)
  - [ ] Executar `pnpm build` pós-refatoração
  - [ ] Registrar novos tamanhos de chunk no Change Log desta story (formato: antes → depois em kb)
  - [ ] Verificar redução ≥ 40% no chunk principal — se não atingida, investigar outros imports pesados no chunk e reportar ao `@ux-design-expert`
  - [ ] Confirmar que `pnpm build` termina sem warnings de bundle size

- [ ] **Task 6: Validação funcional e de performance** (AC: 8, 9, 10)
  - [ ] `pnpm test:run` — todos os testes devem passar (atenção especial ao `IssueChatThread.test.tsx` e testes de `IssueDetail`)
  - [ ] Verificação manual: navegar para `/agents/:id`, `/agents/:id/:tab`, `/agents/:id/runs/:runId` — confirmar que skeleton aparece brevemente e o componente carrega corretamente
  - [ ] Verificação manual: abrir uma Issue com thread ativo — confirmar que skeleton de chat aparece e o thread carrega corretamente
  - [ ] Lighthouse audit na rota `/agents/:id` — registrar LCP antes e depois; variação máxima permitida: +200ms

## Dev Notes

### Contexto da Story

Story implementa **Bloco A — Bundle Size Critical** do EPIC-005. Os dois maiores componentes do bundle foram identificados no Discovery ORION:

- `AgentDetail.tsx` — **176kb** (não minificado), em `ui/src/pages/AgentDetail.tsx`
- `IssueChatThread.tsx` — **158kb** (não minificado), em `ui/src/components/IssueChatThread.tsx`

Juntos, esses dois componentes representam uma parcela significativa do bundle inicial, causando TTI (Time to Interactive) elevado mesmo em navegações que não precisam desses componentes.

### Paths confirmados (verificados via grep e listagem de diretório)

```
ui/src/App.tsx                              # imports de AgentDetail na linha 11; rotas nas linhas 98–100
ui/src/pages/AgentDetail.tsx                # componente alvo — 176kb
ui/src/components/IssueChatThread.tsx       # componente alvo — 158kb
ui/src/pages/IssueDetail.tsx               # consumidor de IssueChatThread (confirmar via grep)
ui/src/components/PageSkeleton.tsx          # skeleton existente — adicionar variantes "agent-detail" e "issue-chat"
```

### Estrutura atual de rotas do AgentDetail em App.tsx

```tsx
// Linhas 98–100 de ui/src/App.tsx (verificado):
<Route path="agents/:agentId" element={<AgentDetail />} />
<Route path="agents/:agentId/:tab" element={<AgentDetail />} />
<Route path="agents/:agentId/runs/:runId" element={<AgentDetail />} />
```

### Padrão de named export com React.lazy

O projeto usa named exports (`export function AgentDetail`), não default exports. O padrão correto de conversão é:

```tsx
// Named export → lazy loading:
const AgentDetail = React.lazy(() =>
  import("./pages/AgentDetail").then((m) => ({ default: m.AgentDetail }))
);
```

### Atenção: IssueChatThread não tem rota própria

`IssueChatThread` é um componente (não uma página) consumido internamente por `IssueDetail.tsx`. O lazy loading deve ser aplicado **no ponto de importação em `IssueDetail.tsx`**, não no `App.tsx`. O Suspense boundary deve ser posicionado de forma a não quebrar o layout existente de `IssueDetail`.

### PageSkeleton.tsx já existe — NÃO recriar

O componente `PageSkeleton.tsx` em `ui/src/components/` já existe com variantes: `list`, `issues-list`, `detail`, `dashboard`, `approvals`, `costs`, `inbox`, `org-chart`. Esta story **adiciona** apenas as variantes `"agent-detail"` e `"issue-chat"` ao arquivo existente. NÃO criar `PageSkeleton` do zero.

### Padrões a seguir

- **Skeleton:** Usar exclusivamente `<Skeleton>` de `@/components/ui/skeleton` com classes Tailwind — sem CSS inline, sem hardcode de cores
- **Design system:** Tokens OKLCH definidos em `ui/src/index.css` (`:root`) — ex: `border-border`, `bg-muted`
- **Testes:** Vitest + Testing Library — padrão já estabelecido no projeto

### Decisões fixas (não eliciar)

- **Escopo restrito:** Esta story aplica lazy loading APENAS em `AgentDetail` e `IssueChatThread`. Não extender para outros componentes nesta iteração.
- **Sem SSR:** Projeto é CSR puro (Vite + React) — não há risco de quebra de SSR com `React.lazy`.
- **Rollback trivial:** Reverter para import direto é uma operação de 1 linha por componente.

### Out of scope (vai em outras stories)

- ❌ Lazy loading de outros componentes → backlog EPIC-005 (fora das 3 stories deste epic)
- ❌ Prefetch de rotas → pode ser avaliado em iteração futura após validação dos ganhos desta story
- ❌ Service Worker / cache de assets → ORION-UI Sprint 2
- ❌ Componentes de estado loading/error/empty → Story 5.2

### Testing

**Arquivos de teste existentes afetados:**
- `ui/src/components/IssueChatThread.test.tsx` — verificar se importação de `IssueChatThread` nos testes precisará de ajuste de mock para lazy loading
- Quaisquer testes de `IssueDetail` que usem `IssueChatThread`

**Comandos de validação obrigatórios:**
- `pnpm test:run`
- `pnpm build`
- Lighthouse audit manual na rota `/agents/:id`

## Change Log

| Date       | Version | Description    | Author          |
|------------|---------|----------------|-----------------|
| 2026-06-17 | 1.0     | Story drafted from EPIC-005 | @sm (River) |

## Dev Agent Record

### Agent Model Used

_A preencher pelo @dev ao iniciar implementação_

### Debug Log References

_A preencher durante implementação_

### Completion Notes List

_A preencher ao concluir_

### File List

| File | Action | Description |
|------|--------|-------------|
| `ui/src/App.tsx` | Modified | Import de AgentDetail convertido para React.lazy(); Suspense boundaries adicionados nas 3 rotas |
| `ui/src/pages/IssueDetail.tsx` | Modified | Import de IssueChatThread convertido para React.lazy(); Suspense boundary adicionado |
| `ui/src/components/PageSkeleton.tsx` | Modified | Variantes "agent-detail" e "issue-chat" adicionadas |

_Preencher com todos os arquivos realmente modificados ao concluir_

## QA Results

**Veredicto:** _Pendente_
**QA Agent:** @ux-design-expert | _Data a preencher_

| Check | Resultado | Notas |
|-------|-----------|-------|
| Bundle analysis (antes → depois) | ⏳ Pendente | Baseline a documentar |
| Skeleton visual (agent-detail) | ⏳ Pendente | |
| Skeleton visual (issue-chat) | ⏳ Pendente | |
| Navegação funcional | ⏳ Pendente | |
| LCP delta (Lighthouse) | ⏳ Pendente | Máx: +200ms |
| `pnpm test:run` | ⏳ Pendente | |
| `pnpm build` | ⏳ Pendente | |
| Acceptance criteria | ⏳ Pendente | 10/10 a verificar |
