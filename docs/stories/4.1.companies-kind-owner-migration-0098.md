# Story 4.1: companies — Colunas `kind` e `owner_user_id` (Migration 0098)

## Status

Ready
**Complexity:** S (Small)

## Executor Assignment

```yaml
executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools: ["schema_validation", "migration_review", "drizzle_typecheck"]
```

**Epic:** EPIC-004 Tech Debt — Database Schema Foundation  
**Position:** 1ª story do EPIC-004 — P0 BLOQUEADOR  
**Bloco coberto:** Bloco A — companies P0 Blockers (D29, D30)  
**Dependência:** Nenhuma (iniciar imediatamente)

## Story

**As a** desenvolvedor implementando o Personal Mode (ORION),  
**I want** que a tabela `companies` tenha os campos `kind` e `owner_user_id`,  
**so that** seja possível distinguir entre companies pessoais e de time, e associar um dono explícito a companies pessoais para controle de acesso e billing.

## Acceptance Criteria

1. A coluna `kind text NOT NULL DEFAULT 'business'` é adicionada à tabela `companies` via migration `0098`
2. A coluna `owner_user_id text REFERENCES "user"(id) ON DELETE SET NULL` é adicionada à tabela `companies` via migration `0098`
3. Um índice único parcial `CREATE UNIQUE INDEX companies_personal_owner_idx ON companies(owner_user_id) WHERE kind = 'personal'` é criado — garantindo que cada usuário tenha no máximo 1 company pessoal
4. O arquivo `packages/db/src/schema/companies.ts` é atualizado com os dois novos campos em Drizzle ORM
5. O rollback plan é documentado no Change Log desta story: `ALTER TABLE companies DROP COLUMN kind; ALTER TABLE companies DROP COLUMN owner_user_id;`
6. `pnpm -r typecheck` passa sem erros após a atualização do schema Drizzle
7. `pnpm test:run` passa sem regressões
8. `pnpm db:generate` gera corretamente a migration SQL a partir do schema Drizzle atualizado
9. Rows existentes em `companies` recebem `kind = 'business'` como valor default (backward compatibility garantida)
10. `docs/db/DB-AUDIT.md` é atualizado: D29 e D30 marcados como `✅ Resolvido`

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Pre-Commit + Pre-PR
>
> - Pre-Commit: Schema validation, `pnpm -r typecheck`
> - Pre-PR: Migration safety check — confirmar que `DEFAULT 'business'` não quebra rows existentes

## Tasks / Subtasks

- [ ] **Task 1: Verificação pré-migration — dados existentes** (AC: 9)
  - [ ] Executar query de diagnóstico para contar rows existentes: `SELECT COUNT(*) FROM companies;`
  - [ ] Confirmar que a tabela não possui coluna `kind` nem `owner_user_id`: `SELECT column_name FROM information_schema.columns WHERE table_name = 'companies';`
  - [ ] Registrar resultado no Change Log desta story

- [ ] **Task 2: Atualizar schema Drizzle em `companies.ts`** (AC: 4, 6)
  - [ ] Abrir `packages/db/src/schema/companies.ts`
  - [ ] Adicionar import de `references` se necessário (verificar se `user` está importado)
  - [ ] Adicionar o campo `kind`:
    ```ts
    kind: text("kind").notNull().default("business"),
    ```
  - [ ] Adicionar o campo `ownerUserId`:
    ```ts
    ownerUserId: text("owner_user_id").references(() => user.id, { onDelete: "set null" }),
    ```
  - [ ] Adicionar o índice único parcial na seção de indexes:
    ```ts
    personalOwnerUniqueIdx: uniqueIndex("companies_personal_owner_idx")
      .on(table.ownerUserId)
      .where(sql`kind = 'personal'`),
    ```
  - [ ] Confirmar que o import de `user` de `./auth.js` está presente (verificar circular dependency)
  - [ ] Executar `pnpm -r typecheck` — deve passar sem erros

- [ ] **Task 3: Gerar e revisar migration `0098`** (AC: 1, 2, 3, 8)
  - [ ] Executar `pnpm db:generate` no diretório `packages/db`
  - [ ] Verificar que o arquivo gerado se chama `0098_*.sql`
  - [ ] Revisar o SQL gerado para confirmar:
    - `ALTER TABLE "companies" ADD COLUMN "kind" text DEFAULT 'business' NOT NULL;`
    - `ALTER TABLE "companies" ADD COLUMN "owner_user_id" text;`
    - `ALTER TABLE "companies" ADD CONSTRAINT "companies_owner_user_id_user_id_fk" FOREIGN KEY ("owner_user_id") REFERENCES "public"."user"("id") ON DELETE set null ON UPDATE no action;`
    - `CREATE UNIQUE INDEX "companies_personal_owner_idx" ON "companies" ("owner_user_id") WHERE kind = 'personal';`
  - [ ] Confirmar que NÃO há `NOT NULL` sem `DEFAULT` em colunas existentes (seria breaking)

- [ ] **Task 4: Validar testes e build** (AC: 6, 7)
  - [ ] `pnpm test:run` — todos os testes devem passar
  - [ ] `pnpm -r typecheck` — zero erros de tipos
  - [ ] Verificar se algum test factory ou seed usa `companies` e precisa de update para incluir `kind`

- [ ] **Task 5: Documentar rollback e atualizar DB-AUDIT.md** (AC: 5, 10)
  - [ ] Registrar rollback plan no Change Log:
    ```sql
    -- Rollback 0098:
    ALTER TABLE companies DROP COLUMN IF EXISTS kind;
    ALTER TABLE companies DROP COLUMN IF EXISTS owner_user_id;
    DROP INDEX IF EXISTS companies_personal_owner_idx;
    ```
  - [ ] Atualizar `docs/db/DB-AUDIT.md`: marcar D29 e D30 como `✅ Resolvido` com referência à migration 0098

## Dev Notes

### Contexto da Story

Esta é a story **P0 bloqueadora** do EPIC-004. Sem `companies.kind`, o Personal Mode (ORION) não pode distinguir companies pessoais de workspaces de time. Sem `companies.owner_user_id`, não há como associar uma company pessoal a um dono para controle de acesso e billing.

[Source: docs/stories/epics/epic-004-tech-debt-db-schema/epic.md#scope]

### Schema atual de `companies` (packages/db/src/schema/companies.ts)

```ts
// Arquivo atual — campos relevantes:
export const companies = pgTable(
  "companies",
  {
    id: uuid("id").primaryKey().defaultRandom(),
    name: text("name").notNull(),
    status: text("status").notNull().default("active"),
    // ... outros campos ...
    createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
    updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(),
  },
  (table) => ({
    issuePrefixUniqueIdx: uniqueIndex("companies_issue_prefix_idx").on(table.issuePrefix),
  }),
);
```

**Não existem** `kind` nem `owner_user_id` atualmente. [Source: packages/db/src/schema/companies.ts verificado]

### Tabela `user` — referência da FK

A tabela `user` usa `id: text` (não UUID) — conforme padrão Better Auth. Portanto `owner_user_id` deve ser `text`, não `uuid`.

```ts
// packages/db/src/schema/auth.ts
export const users = pgTable("user", {
  id: text("id").primaryKey(),
  // ...
});
```

[Source: packages/db/src/schema/auth.ts verificado]

### Migration atual

Última migration aplicada: `0097_serious_inhumans.sql`. A próxima será `0098`. Geração via `pnpm db:generate` em `packages/db/`.

[Source: packages/db/src/migrations/ verificado]

### Padrão de geração de migration no projeto

```bash
# Gera migration SQL a partir do schema Drizzle:
pnpm db:generate

# Aplica em dev (PGlite):
pnpm dev  # server.ts detecta e aplica automaticamente

# Verifica tipos:
pnpm -r typecheck
```

### Risco: circular dependency entre companies.ts e auth.ts

Se `auth.ts` já importa de `companies.ts`, importar `user` em `companies.ts` cria circular dependency. Verificar antes de adicionar o import:

```bash
grep -r "import.*companies" packages/db/src/schema/auth.ts
```

Se houver circular, usar referência lazy do Drizzle:
```ts
ownerUserId: text("owner_user_id").references((): AnyPgColumn => user.id, { onDelete: "set null" }),
```

O padrão de referência lazy já existe em `heartbeat_runs.ts` com o import `AnyPgColumn`. [Source: packages/db/src/schema/heartbeat_runs.ts linha 1]

### Índice único parcial — sintaxe Drizzle

Para índice condicional (WHERE clause) em Drizzle:

```ts
// Requer import de { sql } from "drizzle-orm"
personalOwnerUniqueIdx: uniqueIndex("companies_personal_owner_idx")
  .on(table.ownerUserId)
  .where(sql`kind = 'personal'`),
```

### Decisões fixas (não eliciar)

- **`kind DEFAULT 'business'`:** Obrigatório para backward compatibility com rows existentes
- **`owner_user_id` nullable:** Apenas companies do tipo `personal` terão owner — companies de time não precisam
- **ON DELETE SET NULL:** Se o usuário dono for deletado, a company pessoal não é deletada mas perde o owner

### Out of scope (vai em outras stories)

- ❌ Lógica de app para criação de personal company → ORION-001
- ❌ FKs de `user_id` em outras tabelas → Story 4.2
- ❌ Tabelas Vault Obsidian → ORION-002

## Change Log

| Date       | Version | Description    | Author          |
|------------|---------|----------------|-----------------|
| 2026-06-17 | 1.0     | Story drafted from EPIC-004 | @sm (River) |
| 2026-06-17 | 1.1     | Validated GO (10/10) — Status: Draft → Ready | @po (Pax) |

## Dev Agent Record

### Agent Model Used

_A preencher pelo @data-engineer ao iniciar implementação_

### Debug Log References

_A preencher durante implementação_

### Completion Notes List

_A preencher ao concluir_

### File List

| File | Action | Description |
|------|--------|-------------|
| `packages/db/src/schema/companies.ts` | Modified | Campos `kind` e `ownerUserId` adicionados; índice `personalOwnerUniqueIdx` |
| `packages/db/src/migrations/0098_*.sql` | Created | Migration gerada via `pnpm db:generate` |
| `docs/db/DB-AUDIT.md` | Modified | D29, D30 marcados como ✅ Resolvido |

## QA Results

**Veredicto:** _Pendente_  
**QA Agent:** @dev | _Data a preencher_

| Check | Resultado | Notas |
|-------|-----------|-------|
| Schema Drizzle válido | ⏳ Pendente | `pnpm -r typecheck` |
| Migration SQL gerada corretamente | ⏳ Pendente | Revisar SQL gerado |
| DEFAULT 'business' backward-safe | ⏳ Pendente | |
| FK para `user.id` declarada | ⏳ Pendente | |
| Índice único parcial criado | ⏳ Pendente | |
| `pnpm test:run` | ⏳ Pendente | |
| Rollback documentado | ⏳ Pendente | |
| DB-AUDIT.md atualizado | ⏳ Pendente | D29, D30 |
