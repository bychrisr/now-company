# Story 1.1: Social Platforms Table and Initial Seed

## Status

Ready
**Complexity:** S (Small)

## Executor Assignment

```yaml
executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools: ["coderabbit", "drizzle-kit", "vitest"]
```

**Epic:** EPIC-001 Platform Foundation
**Position:** 1ª story (base do epic — desbloqueia 1.2, 1.3, 1.4)
**Blocks coberto:** Bloco 1 — Platform Registry (apenas a tabela + seed inicial mínimo)

## Story

**As a** plataforma Paperclip,
**I want** uma tabela `social_platforms` global no banco com seed inicial de plataformas essenciais,
**so that** outras entidades (`company_social_accounts`, futuras `content_pieces`) possam referenciar plataformas com integridade referencial, e o Super Admin tenha base para gerenciar o catálogo na UI (Story 1.2).

## Acceptance Criteria

1. Existe nova migration `0094_*.sql` em `packages/db/src/migrations/` criando a tabela `social_platforms` com os campos especificados na seção Dev Notes.
2. Existe arquivo `packages/db/src/schema/social_platforms.ts` definindo a tabela com Drizzle ORM, seguindo o padrão dos schemas existentes (ex: `companies.ts`, `agents.ts`).
3. O schema é exportado em `packages/db/src/schema/index.ts`.
4. A tabela `social_platforms` é GLOBAL (não tem `company_id`) — é catálogo da instância, gerido pelo Super Admin.
5. A tabela tem unique constraint em `slug` (kebab-case, único globalmente).
6. Existe seed inicial em `packages/db/src/seed.ts` (ou arquivo dedicado importado no seed) populando ao menos 5 plataformas essenciais para validar o schema: `instagram`, `youtube`, `linkedin`, `tiktok`, `twitter-x`.
7. O seed é idempotente — rodar 2x não duplica registros (usa `onConflictDoNothing` ou upsert por slug).
8. Comando `pnpm run db:migrate` aplica a migration sem erros.
9. Comando `pnpm run db:seed` insere as plataformas sem erros, e rodar 2x não duplica.
10. Comando `pnpm run typecheck` passa sem erros.
11. Comando `pnpm run build` passa sem erros.

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type:** Database
**Secondary Type(s):** —
**Complexity:** Low (1 tabela, sem joins complexos, sem RLS — tabela global)

### Specialized Agent Assignment

**Primary Agents:**
- @data-engineer (Dara) — schema design + migration + seed
- @dev (Dex) — code review pré-commit

**Supporting Agents:**
- @qa (Quinn) — validação final antes de merge

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Run before marking story complete
- [ ] Pre-PR (@github-devops): Run before creating pull request
- [ ] Pre-Deployment: N/A (mudança de schema, não toca código de produção runtime ainda)

### Self-Healing Configuration

**Expected Self-Healing:**
- Primary Agent: @data-engineer (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL, HIGH

**Predicted Behavior:**
- CRITICAL issues: auto_fix (ex: missing index, unique constraint inválida)
- HIGH issues: document_only se não auto-fixável

### CodeRabbit Focus Areas

**Primary Focus:**
- Schema compliance: tipos corretos, defaults sanos, índices em campos consultáveis
- Migration safety: sem operação destrutiva, idempotente

**Secondary Focus:**
- Seed idempotência: rodar 2x produz mesmo estado
- Padrão Drizzle consistente com schemas existentes

## Tasks / Subtasks

- [ ] **Task 1: Criar schema Drizzle `social_platforms.ts`** (AC: 2, 3, 4, 5)
  - [ ] Criar arquivo `packages/db/src/schema/social_platforms.ts`
  - [ ] Seguir padrão de `packages/db/src/schema/companies.ts` (imports, formatação, índices)
  - [ ] Adicionar export em `packages/db/src/schema/index.ts`
  - [ ] Garantir unique index em `slug`

- [ ] **Task 2: Gerar migration via Drizzle Kit** (AC: 1, 8)
  - [ ] Rodar `pnpm run db:generate` (ou comando equivalente do projeto) para gerar migration
  - [ ] Validar que migration gerada é `0094_*.sql`
  - [ ] Revisar SQL gerado: tipos, defaults, unique constraint
  - [ ] Aplicar localmente: `pnpm run db:migrate` — deve passar limpo

- [ ] **Task 3: Criar seed inicial de plataformas essenciais** (AC: 6, 7, 9)
  - [ ] Criar arquivo `packages/db/src/seed-social-platforms.ts` OU adicionar bloco em `seed.ts` existente
  - [ ] Popular 5 plataformas: instagram, youtube, linkedin, tiktok, twitter-x
  - [ ] Usar `onConflictDoNothing({ target: socialPlatforms.slug })` para idempotência
  - [ ] Para cada plataforma: `slug`, `name`, `category`, `status: 'enabled'`, `capabilities: {}` (vazio nesta story — será expandido em 1.3)
  - [ ] Rodar 2x e validar zero duplicação via `SELECT COUNT(*) FROM social_platforms`

- [ ] **Task 4: Validação completa** (AC: 10, 11)
  - [ ] `pnpm run typecheck` passa
  - [ ] `pnpm run build` passa
  - [ ] Testar query: `SELECT slug, name FROM social_platforms` retorna 5 linhas
  - [ ] Verificar se outros schemas em `packages/db/src/schema/*.ts` têm arquivos `.test.ts` correspondentes. Se SIM, criar `social_platforms.test.ts` seguindo o mesmo padrão. Se NÃO, pular testes de schema (segue convenção do projeto).

## Dev Notes

### Origem da Story

Esta story implementa parte do **Bloco 1 — Platform Registry** descrito em `docs/plans/content-brand-module-brainstorm.md`. O atlas técnico completo (`docs/references/global-content-formats-atlas.md` v3.1) tem specs detalhados de 15+ plataformas — esses specs entram na Story 1.3 (seed expandido), não nesta.

### Schema proposto da tabela `social_platforms`

```typescript
// packages/db/src/schema/social_platforms.ts
import { pgTable, uuid, text, timestamp, jsonb, index, uniqueIndex } from "drizzle-orm/pg-core";

export const socialPlatforms = pgTable(
  "social_platforms",
  {
    id: uuid("id").primaryKey().defaultRandom(),

    // Identificação
    slug: text("slug").notNull(), // 'instagram', 'youtube' — único global
    name: text("name").notNull(), // 'Instagram', 'YouTube' — display
    category: text("category").notNull(), // 'social', 'video', 'audio', 'blog', 'community'

    // Governança Super Admin
    status: text("status").notNull().default("enabled"), // enabled | disabled
    sortOrder: integer("sort_order").notNull().default(0),

    // Specs técnicos (expandido na Story 1.3)
    capabilities: jsonb("capabilities").$type<Record<string, unknown>>().notNull().default({}),
    copySpecs: jsonb("copy_specs").$type<Record<string, unknown>>().notNull().default({}),
    imageSpecs: jsonb("image_specs").$type<Record<string, unknown>>().notNull().default({}),

    // Metadata
    iconUrl: text("icon_url"),
    description: text("description"),
    websiteUrl: text("website_url"),

    createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(),
    updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow(),
  },
  (table) => ({
    slugUq: uniqueIndex("social_platforms_slug_uq").on(table.slug),
    statusSortIdx: index("social_platforms_status_sort_idx").on(table.status, table.sortOrder),
    categoryIdx: index("social_platforms_category_idx").on(table.category),
  }),
);
```

**Justificativas (porquê):**
- `slug` é PK lógica para uso em código (`platform_id` em FKs futuras). UUID `id` é PK física para joins eficientes.
- `capabilities/copy_specs/image_specs` são JSONB porque cada plataforma tem estrutura própria — schema rígido seria contraproducente. Story 1.3 define os shapes específicos via TypeScript.
- Sem `company_id`: tabela GLOBAL gerida pelo Super Admin (Bloco 0 → Story 1.2).
- Sem soft delete nesta story — `status: 'disabled'` cobre o caso de uso.

### Padrões a seguir

- **Schema:** seguir formato de `packages/db/src/schema/companies.ts` (imports, export, índices nomeados com pattern `{table}_{cols}_{type}`).
- **Migration:** Drizzle Kit gera automaticamente — não editar manualmente o `.sql` exceto se necessário (e documentar o porquê).
- **Seed:** seguir padrão de `packages/db/src/seed.ts` existente (usa `db.insert(...).values(...)`).

### Seed mínimo proposto (5 plataformas)

```typescript
const initialPlatforms = [
  { slug: "instagram", name: "Instagram", category: "social", sortOrder: 10 },
  { slug: "youtube", name: "YouTube", category: "video", sortOrder: 20 },
  { slug: "linkedin", name: "LinkedIn", category: "social", sortOrder: 30 },
  { slug: "tiktok", name: "TikTok", category: "video", sortOrder: 40 },
  { slug: "twitter-x", name: "Twitter/X", category: "social", sortOrder: 50 },
];

await db.insert(socialPlatforms).values(initialPlatforms).onConflictDoNothing({
  target: socialPlatforms.slug,
});
```

### Out of scope nesta story (vai em outras stories)

- ❌ Specs técnicos detalhados das plataformas (copy_specs, image_specs preenchidos) → **Story 1.3**
- ❌ 15+ plataformas no seed → **Story 1.3**
- ❌ UI Super Admin para CRUD → **Story 1.2**
- ❌ Tabela `company_social_accounts` que referencia esta → **Story 1.4**
- ❌ Ícones reais por plataforma → **Story 1.3**

### Testing

**Test file location:** `packages/db/src/schema/social_platforms.test.ts` (se houver padrão de testes de schema no projeto — verificar se outros schemas têm `.test.ts` antes de criar).

**Test standards:**
- Vitest (padrão do projeto).
- Testar: insert duplicado (slug) deve falhar com unique constraint violation.
- Testar: seed rodado 2x não duplica.

**Frameworks:** Vitest, Drizzle ORM test patterns.

**Comandos de validação obrigatórios:**
- `pnpm run typecheck`
- `pnpm run build`
- `pnpm run db:migrate` (em ambiente local de dev)

## Change Log

| Date       | Version | Description    | Author          |
|------------|---------|----------------|-----------------|
| 2026-05-30 | 1.0     | Story drafted from EPIC-001 | @sm (River) |
| 2026-05-30 | 1.1     | PO validation passed (8/10), status Approved→Ready, complexity S | @po (Pax) |

## Dev Agent Record

### Agent Model Used

_To be populated by @data-engineer during implementation._

### Debug Log References

_To be populated during implementation._

### Completion Notes List

_To be populated upon completion._

### File List

_To be populated upon completion._

## QA Results

_To be populated by @qa after implementation._
