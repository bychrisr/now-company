# Story 1.5: OAuth Flow + Company Secrets Integration

> ⚠️ **STORY CRÍTICA — RISCO ALTO** ⚠️
>
> Esta story toca dois sistemas sensíveis simultaneamente: **OAuth de terceiros** e **storage de secrets**.
> Qualquer vazamento de token (logs, queries, error responses) é incidente de segurança.
> **OBRIGATÓRIO antes do merge:** code review com @architect + @qa pair-review.
>
> Decisão imutável: tokens NUNCA na tabela `company_social_accounts`, sempre via `secret_id → company_secrets`.

## Status

Done
**Complexity:** L (Large)

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["typescript", "vitest"]
```

**Epic:** EPIC-001 Platform Foundation
**Position:** 5ª story do EPIC-001
**Bloco coberto:** Bloco 4A (OAuth flow + integração company_secrets)
**Dependência:** Story 1.4 (tabela `company_social_accounts`), `company_secrets` (existente)

## Story

**As a** empresa do Paperclip,
**I want** conectar minhas contas sociais via OAuth (a partir do Instagram como primeira plataforma),
**so that** os tokens fiquem isolados no cofre `company_secrets` e a tabela `company_social_accounts` apenas referencie via `secret_id`.

## Acceptance Criteria

1. Existe endpoint `POST /api/company/social-accounts/connect/:platformSlug` que inicia OAuth flow:
   - Gera state token (para CSRF)
   - Redireciona para URL de autorização da plataforma
   - Valida permissão de quem chama (membro da empresa)
2. Existe callback `GET /api/oauth/callback/:platformSlug?code=...&state=...`:
   - Valida state token
   - Troca `code` por `access_token` + `refresh_token`
   - Cria registro em `company_secrets` (key: `oauth_{platformSlug}_{platformAccountId}`)
   - Cria/atualiza registro em `company_social_accounts` com `secret_id` apontando para o secret recém-criado
   - Redireciona para UI de sucesso
3. Endpoint `DELETE /api/company/social-accounts/:id`:
   - Soft delete via `is_active = false`
   - Marca secret como `status = 'revoked'` em `company_secrets` (NÃO deletar — mantém audit trail)
4. Endpoint `GET /api/company/social-accounts` — lista contas da empresa logada (com `is_active = true` por padrão).
5. **Implementar fluxo completo APENAS para Instagram** nesta story. Outras plataformas viram stories próprias quando necessário (1.5.x).
6. Isolamento: usuário da empresa A não consegue listar/conectar/desconectar contas da empresa B.
7. Tokens criptografados via mecanismo existente do `company_secrets` (sem reimplementar criptografia).
8. `pnpm run typecheck`, `pnpm run build`, `pnpm run test` passam.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled — manual review CRÍTICA pois envolve OAuth + secrets.

## Tasks / Subtasks

- [x] **Task 1: App Instagram + variáveis de ambiente** (AC: 1, 2)
  - [x] Documentar setup: criar Meta App para Instagram Graph API
  - [x] Variáveis em `.env`: `INSTAGRAM_APP_ID`, `INSTAGRAM_APP_SECRET`, `INSTAGRAM_REDIRECT_URI`
  - [x] Adicionar entrada em `server/src/config.ts` para validar presença em runtime

- [x] **Task 2: State token (CSRF protection)** (AC: 1, 2)
  - [x] Implementar geração/validação de state usando lib existente (verificar `server/src/lib/`)
  - [x] State curto-prazo (expira em 10 min)
  - [x] Storage temporário (DB ou cache em memória — verificar padrão do projeto)

- [x] **Task 3: Endpoint connect** (AC: 1, 6)
  - [x] Criar `server/src/routes/social-accounts.ts`
  - [x] `POST /companies/:companyId/social-accounts/connect/:platformSlug`
  - [x] Auth: `assertBoard` + `assertCompanyAccess` — middleware existente
  - [x] Para Instagram: monta URL Graph API OAuth com `client_id`, `redirect_uri`, `scope`, `state`
  - [x] Retorna `{ authUrl: "..." }` (UI redireciona o usuário)

- [x] **Task 4: Endpoint callback** (AC: 2, 7)
  - [x] `GET /oauth/callback/:platformSlug` em `server/src/routes/oauth-callback.ts`
  - [x] Valida state (CSRF)
  - [x] POST para Instagram `/oauth/access_token` com `code`
  - [x] Recebe `access_token`, `user_id`
  - [x] Cria `company_secrets` row via `secretService.create()`
  - [x] Cria/atualiza `company_social_accounts` com `secret_id`, `platform_account_id`, `handle` (fetch via `/me`)
  - [x] Redireciona para UI: `/company/settings/social-accounts?status=connected&platform=instagram`

- [x] **Task 5: Endpoint disconnect e list** (AC: 3, 4, 6)
  - [x] `DELETE /companies/:companyId/social-accounts/:id` — soft delete + revoke secret
  - [x] `GET /companies/:companyId/social-accounts?active=true` — query params para filtro
  - [x] Isolamento via `company_id` do contexto autenticado em TODAS as queries

- [x] **Task 6: Testes de isolamento** (AC: 6)
  - [x] Test: usuário empresa A tenta GET conta de empresa B → 403
  - [x] Test: empresa A tenta DELETE conta da empresa B → 404
  - [x] Test: callback com state inválido → 400
  - [x] Test: disconnect marca `is_active=false` e secret como `revoked`

- [x] **Task 7: Validação final**
  - [x] `pnpm run typecheck` — limpo
  - [x] `pnpm run build` — limpo (exit code 0)
  - [x] `pnpm run test` — todos passaram (exit code 0)
  - [ ] Smoke test manual: conectar Instagram real (em ambiente dev) → requer Meta App configurado

## Dev Notes

### Decisão crítica (não negociar)
Tokens **NUNCA** ficam em `company_social_accounts.access_token`. Sempre via `secret_id` → `company_secrets`. Decisão registrada em `docs/plans/content-brand-module-brainstorm.md` Bloco 4A.

### Infra existente a reusar
- `server/src/secrets/` — módulo de secrets do Paperclip
- `company_secret_provider_configs` — config de provider OAuth por empresa (se aplicável)
- `server/src/services/authorization.ts` — middleware de auth de empresa
- `secret_access_events` — audit log automático ao acessar secrets

### Plataforma única nesta story
Instagram primeiro porque:
- API estável (Graph API)
- Fluxo OAuth bem documentado
- Plataforma prioritária para o produto

Outras plataformas (YouTube, LinkedIn, TikTok) viram stories 1.5.1, 1.5.2, etc. quando necessárias. NÃO tentar generalizar prematuramente nesta story.

### Out of scope
- ❌ UI de connect/disconnect → Story 1.7
- ❌ Sync de métricas → Story 1.6
- ❌ Publicação de conteúdo → Epic 3
- ❌ OAuth de outras plataformas → stories futuras

### Testing
- Mock do endpoint OAuth Instagram em testes (não bater na API real em CI)
- Test isolamento por `company_id` é OBRIGATÓRIO antes de marcar story como Done

## Change Log

| Date       | Version | Description    | Author          |
|------------|---------|----------------|-----------------|
| 2026-05-30 | 1.0     | Story drafted from EPIC-001 (batch mode) | @sm (River) |
| 2026-05-30 | 1.1     | PO validation passed (8/10), status Approved→Ready, complexity L, criticality alert added | @po (Pax) |

## Dev Agent Record

### Files Created/Modified
- `server/src/routes/social-accounts.ts` — endpoints list, connect, disconnect + state token store
- `server/src/routes/oauth-callback.ts` — Instagram callback: code exchange, secret creation, account upsert
- `server/src/config.ts` — `instagramAppId`, `instagramAppSecret`, `instagramRedirectUri`
- `server/src/app.ts` — registro das novas rotas
- `server/src/routes/index.ts` — exports
- `server/src/__tests__/social-accounts.test.ts` — 6 testes de isolamento

### Completion Notes
- State token store em memória com TTL 10 min + cleanup interval a cada 5 min
- Tokens OAuth armazenados via `secretService.create()` — jamais inline na tabela `company_social_accounts`
- Upsert seguro: reconnecting a mesma conta reaproveita o secret existente (chave determinística `oauth_instagram_{id}`)
- Smoke test manual requer criação de Meta App com Graph API habilitada — fora de CI

## QA Results

**Veredicto final: PASS** ✅

### Revisão 1 (2026-05-31) — CONCERNS
Issues identificados pelo @qa:
- **HIGH**: Re-autenticação não rotacionava o token → **RESOLVIDO** por @dev
- **MEDIUM**: `access_token` como query param no `/me` → **RESOLVIDO** por @dev
- **MEDIUM**: Sem rate limiting no `/connect` → **Debt documentado** (não bloqueia)

### Revisão 2 (2026-05-31) — PASS
Fixes verificados:
- ✅ Re-auth agora reativa secret se necessário e chama `secrets.rotate()` com novo token
- ✅ `fetchInstagramMe` usa `Authorization: Bearer` header — token fora de query params
- ✅ Novo teste: double-disconnect de conta inativa retorna 400
- ✅ 7/7 testes passando

### Debt técnico remanescente
- Rate limiting por `companyId` no endpoint `/connect` (baixo risco, futuro)

### AC Traceability: 8/8 ✅

*Revisão 1 por @qa (Quinn) em 2026-05-31 — CONCERNS*
*Revisão 2 por @qa (Quinn) em 2026-05-31 — PASS*
